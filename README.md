# Mutation Load Visualizer

A simple web app to upload variant tables (TSV) and visualize mutation load along genomic regions.

## Local test

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
